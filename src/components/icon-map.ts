import { EyeOff, ShieldCheck, Building2, Network, Landmark, Lock, Wallet, type LucideIcon } from 'lucide-react'
import type { DiagramNode } from '../data/nodes'

export const ICON_MAP: Record<DiagramNode['icon'], LucideIcon> = {
  EyeOff,
  ShieldCheck,
  Building2,
  Network,
  Landmark,
  Lock,
  Wallet,
}
